# mdBook tutorial track ("trem by example")

## What

A `book/` or `docs/book/` mdBook with chapters: rationals & trees, first grid, first graph, Rung round-trip, offline render, TUI tour.

## Why

README is reference-heavy; a narrative track helps self-learners and classroom use.

## Notes

- Prefer linking to existing `crates/trem/examples/` rather than duplicating code.
- CI: `mdbook build` in GitHub Actions.
