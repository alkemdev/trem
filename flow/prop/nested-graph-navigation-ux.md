# Nested graph navigation UX pass

## What

User-test the drill-in/drill-out model for graphs-as-processors: breadcrumbs, keyboard consistency, whether to show port wiring across nesting levels, default focus on enter/exit.

## Why

Nested graphs are a differentiator but cognitively heavy; polish here signals maturity.

## Notes

- Add scenarios to `docs/tui-testing.md` or keyboard flow tests.
- Consider a "flat list" alternate view for debugging large patches.
