# Euclidean rhythm as a first-class UI affordance

## What

Beyond the `euclidean` API example: a TUI widget or pattern mode to set hits/steps and hear immediate results, optionally paste into the grid.

## Why

Euclidean rhythms are a signature "math music" feature; surfacing them teaches the engine and speeds composition.

## Notes

- Could live in pattern editor as a "generate fill" action.
- Document interaction with irregular meters if any.
