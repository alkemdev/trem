# Done

Completed work items: implementation merged, tests and verification done.

Move files here from **`../work/`**. Append a **Lessons learned** section (what worked,
what surprised you, what to do differently next time).

This folder is the **flow decision log** for shipped changes. It is **not** the same as
top-level **`docs/`**, which holds ongoing user and architecture documentation
(`install.md`, `graph-architecture.md`, etc.).

**Archives** (completion write-ups, obsolete proposals after merge) also live here so
**`docs/`** does not accumulate one-off “pass complete” reports — e.g.
`documentation-and-examples-pass.md`, `trem-dsp-crate-split.md`.
