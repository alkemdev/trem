# Plan

Accepted direction, **detailed** implementation design: steps, edge cases, file-level
touch list, optional diagrams.

Move a document here from **`../todo/`** when you expand it into an executable plan.
When implementation starts, move it to **`../work/`** and add a **Status log** section
to the markdown.
